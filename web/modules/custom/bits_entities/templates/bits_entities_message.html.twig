{#
/**
 * @file bits_entities_message.html.twig
 * Default theme implementation to present Bits Entities Message data.
 *
 * This template is used when viewing Bits Entities Message pages.
 *
 *
 * Available variables:
 * - content: A list of content items. Use 'content' to print all content, or
 * - attributes: HTML attributes for the container element.
 *
 * @see template_preprocess_bits_entities_message()
 *
 * @ingroup themeable
 */
#}
<div{{ attributes.addClass('bits_entities_message') }}>
  {% if content %}
    {{- content -}}
  {% endif %}
</div>
